# =============================================================================
# Project dependencies (production / runtime)
# =============================================================================
# 설치: pip install -r requirements.txt
# 개발 도구는 requirements-dev.txt 참고

# -----------------------------------------------------------------------------
# PyTorch (CUDA 12.8) — 환경에 맞춰 주석 해제 후 사용
# 서버/머신마다 CUDA 버전이 다르므로 기본은 비활성화 상태
# -----------------------------------------------------------------------------
# --extra-index-url https://download.pytorch.org/whl/cu128
# torch==2.10.0+cu128
# torchaudio==2.10.0+cu128
# torchvision==0.25.0+cu128

# -----------------------------------------------------------------------------
# 핵심 과학 계산
# -----------------------------------------------------------------------------
numpy
pandas
scipy
scikit-learn

# -----------------------------------------------------------------------------
# 시각화
# -----------------------------------------------------------------------------
matplotlib
seaborn

# -----------------------------------------------------------------------------
# 노트북
# -----------------------------------------------------------------------------
jupyter
ipykernel

# -----------------------------------------------------------------------------
# 실험 추적
# -----------------------------------------------------------------------------
wandb

# -----------------------------------------------------------------------------
# 설정 관리
# -----------------------------------------------------------------------------
pyyaml

# -----------------------------------------------------------------------------
# 로깅 / 유틸
# -----------------------------------------------------------------------------
structlog
tqdm
python-dotenv

# -----------------------------------------------------------------------------
# ASR — 평가
# -----------------------------------------------------------------------------
jiwer                  # CER / WER 계산 (corpus-level)

# -----------------------------------------------------------------------------
# ASR — 모델 (SenseVoice / Whisper)
# -----------------------------------------------------------------------------
funasr                 # SenseVoice 학습/추론 (alibaba FunAudioLLM)
transformers           # Whisper (HuggingFace)
accelerate             # Whisper 학습 (HF Trainer 의존)
datasets               # Whisper 학습 (HF Dataset)

# -----------------------------------------------------------------------------
# 오디오 I/O
# -----------------------------------------------------------------------------
soundfile              # wav 로드 (Whisper 어댑터 등)
librosa                # mel spectrogram / 리샘플링 등 (선택, 노트북 분석용)

# -----------------------------------------------------------------------------
# 메타시트 처리
# -----------------------------------------------------------------------------
openpyxl               # Corpora_Meta_Table.xlsx / Benchmark_Meta_Table.xlsx 입출력
